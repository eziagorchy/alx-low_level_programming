#include <stdio.h>
/**
  * main - Entry point of program
  * printf: Print a given string
  * Return: Always returns 0
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
